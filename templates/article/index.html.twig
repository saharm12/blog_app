{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}

    <div class="card">
  <div class="card-header">
    Articles
  </div>
  <div class="card-body">
    <h5 class="card-title">Application pour la gestion des articles</h5>
                 <a href="{{ path('app_article_new') }}" class="btn btn-primary">
                  <i class="fa fa-plus" aria-hidden="true"></i>
                 Nouvel article</a>
  </div>
</div>

</div>
            
    <table class="table table-striped" >
        <thead>
            <tr>
                <th>Id</th>
                <th>Date_publication</th>
                <th>Titre</th>
                <th>Contenu</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for article in articles %}
            <tr>
                <td>{{ article.id }}</td>
                <td>{{ article.datePublication ? article.datePublication|date('Y-m-d') : '' }}</td>
                <td>{{ article.titre }}</td>
                <td>{{ article.contenu }}</td>
                <td>

                    <a href="{{ path('app_article_show', {'id': article.id}) }}" class="btn btn-outline-primary">
                       <i class="fa fa-eye" aria-hidden="true"></i></a>
                    <a href="{{ path('app_article_edit', {'id': article.id}) }}" class="btn btn-outline-success">
                    <i class="fa fa-pencil-square-o" aria-hidden="true"></i>
                    </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">Aucun résultat</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}